{
  "extends": "@react-native/typescript-config",
  "compilerOptions": {
    "strict": true,
    // Base config wants @types/jest, which isn't installed (no test setup yet)
    "types": []
  },
  "include": ["App.tsx", "src/**/*.ts", "src/**/*.tsx"],
  "exclude": ["node_modules", "android", "ios"]
}
