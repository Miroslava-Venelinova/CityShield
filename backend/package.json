{
  "name": "cityshield-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "seed:generate": "node seeds/generate-seed.mjs",
    "db:local": "wrangler d1 migrations apply cityshield-db --local && node seeds/generate-seed.mjs && wrangler d1 execute cityshield-db --local --file=seeds/seed.sql",
    "db:remote": "wrangler d1 migrations apply cityshield-db --remote && node seeds/generate-seed.mjs && wrangler d1 execute cityshield-db --remote --file=seeds/seed.sql",
    "db:reseed:local": "wrangler d1 migrations apply cityshield-db --local && node seeds/generate-seed.mjs && wrangler d1 execute cityshield-db --local --file=seeds/reseed-pre.sql && wrangler d1 execute cityshield-db --local --file=seeds/seed.sql && wrangler d1 execute cityshield-db --local --file=seeds/reseed-post.sql",
    "db:reseed:remote": "wrangler d1 migrations apply cityshield-db --remote && node seeds/generate-seed.mjs && wrangler d1 execute cityshield-db --remote --file=seeds/reseed-pre.sql && wrangler d1 execute cityshield-db --remote --file=seeds/seed.sql && wrangler d1 execute cityshield-db --remote --file=seeds/reseed-post.sql"
  },
  "dependencies": {
    "hono": "^4.8.0",
    "jsts": "^2.12.1",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.19",
    "@cloudflare/workers-types": "^4.20260701.0",
    "cheerio": "^1.2.0",
    "typescript": "^5.5.0",
    "vitest": "~3.2.0",
    "wrangler": "~4.36.0"
  },
  "allowScripts": {
    "esbuild@0.28.1": true,
    "esbuild@0.25.4": true,
    "sharp@0.33.5": true,
    "workerd@1.20250906.0": true
  }
}
